from setuptools import setup, find_packages

setup(
    name='brewassist_core',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        # Add any dependencies here, e.g., 'llama-cpp-python',
    ],
)
