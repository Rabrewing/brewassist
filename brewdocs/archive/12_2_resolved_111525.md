Got you. Here’s a combined, cleaned, “hand this to Gemini” version that merges what Co-P said and what I added, with exact steps to get Test 12.2 passing.

You can literally copy-paste this to him as BREWASSIST_CORE_MODULE_FIX.md if you want.

---

BrewAssist Core – Module Resolution Fix for brewassist_core

0. Problem Snapshot

Goal: Make this pass Test 12.2

python -m brewassist_core.agents.codegen_runner "Write a Python function to add two numbers"

Current error:

ModuleNotFoundError: No module named 'brewassist_core'

Things already tried (but still failing):

Setting PYTHONPATH=/home/brewexec

Adding **init**.py in brewassist-core/ and brewassist-core/agents/

pip install -e /home/brewexec/brewassist-core

Manual sys.path hacks

Running codegen_runner.py directly → relative import error

Root cause:
Python can’t see a real package called brewassist_core that matches the import path, and the repo folder is hyphenated (brewassist-core) which is not a valid Python module name.

---

1. Canonical Decision: What We Want

For all tools (BrewAssist, BrewRouter, Gemini CLI, Co-P, shell scripts), the canonical Python import should be:

import brewassist_core
from brewassist_core.agents import codegen_runner

and the CLI entrypoint should be:

python -m brewassist_core.agents.codegen_runner "some prompt"

So we need a real package tree that looks like:

/home/brewexec/
brewassist_core/
**init**.py
agents/
**init**.py
codegen_runner.py
logger.py
persona.py
selector.py
planner.py
file_agent.py

Everything else (repo names, hyphens, etc.) is optional sugar.

---

2. Quick Fix (Recommended): Rename + Normalize Package

🔹 Step 1 — Rename the folder to match the package

If the current repo folder is:

/home/brewexec/brewassist-core/

rename it so the code lives under brewassist_core (underscore):

cd /home/brewexec

# If brewassist-core currently contains the code directly:

mv brewassist-core brewassist_core

If you want to keep brewassist-core/ as the git root and just move the Python package inside, use this instead:

cd /home/brewexec

mkdir -p brewassist_core
mv brewassist-core/agents brewassist_core/

# (and move any other core files you want under brewassist_core/)

Either way, at the end you want:

ls /home/brewexec/brewassist_core

# should show: agents/ (and maybe other core files)

---

🔹 Step 2 — Ensure **init**.py files exist

From /home/brewexec:

touch brewassist_core/**init**.py
touch brewassist_core/agents/**init**.py

Now the package structure is:

/home/brewexec/
brewassist_core/
**init**.py
agents/
**init**.py
codegen_runner.py
...

---

🔹 Step 3 — Use the venv’s Python from /home/brewexec

Activate the venv and test imports from the project root:

cd /home/brewexec
source .venv/bin/activate # or your usual activate script

python -c "import brewassist_core; print('OK:', brewassist_core.**file**)"

Expected:

OK: /home/brewexec/brewassist_core/**init**.py

If that works, run Test 12.2 again:

python -m brewassist_core.agents.codegen_runner "Write a Python function to add two numbers"

If Mistral GGUF is reachable, you should see:

# ── BrewExec Fallback Chain ──

# ── Fallback: GGUF (Mistral) ──

...

If GGUF fails, you should drop to:

# ── Fallback: distilgpt2 ──

...

or the stub.

---

3. Editable Install (Optional, but Nice for Tools)

If you want brewassist_core to be importable from anywhere while the venv is active (not just /home/brewexec), you can add a tiny setup.py or pyproject.toml inside the package folder and do an editable install.

Minimal setup.py (optional)

In /home/brewexec/brewassist_core/setup.py:

from setuptools import setup, find_packages

setup(
name="brewassist_core",
version="0.1.0",
packages=find_packages(),
)

Then:

cd /home/brewexec/brewassist_core
/home/brewexec/.venv/bin/pip install -e .

But to be clear: this is optional. If your working directory is /home/brewexec and the venv is active, Python will see brewassist_core just from the folder layout alone.

---

4. Clean Up codegen_runner.py (No More sys.path Hacks)

Inside brewassist_core/agents/codegen_runner.py:

1. Make sure the very first line is:

from **future** import annotations

2. Then the imports should simply be:

from llama_cpp import Llama
from transformers import AutoTokenizer, AutoModelForCausalLM
from . import logger

3. Remove any sys.path.insert(...) hacks you previously added. They’re no longer needed and can actually mask real problems.

As long as the package tree is correct and you’re in /home/brewexec with the venv active, relative imports like from . import logger will work.

---

5. Align Shell Scripts With the New Layout

Anywhere we currently call:

python3 ~/brewexec/codegen_runner.py "$prompt"

we should standardize on:

python3 -m brewassist_core.agents.codegen_runner "$prompt"

Examples:

brewagent_mistral.sh

#!/usr/bin/env bash

# ── BrewExec Agent: Mistral Fallback ──

PROMPT="$\*"

if [ -z "$PROMPT" ]; then
echo "Usage: brewagent_mistral.sh <prompt>" >&2
exit 1
fi

echo "# ── Gemini CLI: Mistral Fallback ──"
python3 -m brewassist_core.agents.codegen_runner "$PROMPT"

brewloop_mistral.sh

#!/usr/bin/env bash

# ── BrewExec Loop: Mistral ──

while true; do
echo -n "🧠 Prompt > "
read -r prompt

if [["$prompt" == "exit"]]; then
break
fi

python3 -m brewassist_core.agents.codegen_runner "$prompt"
echo
done

brewlog.sh

#!/usr/bin/env bash

# ── BrewExec Logger ──

timestamp=$(date +"%Y-%m-%d_%H-%M-%S")
logdir="$HOME/brewexec/logs"
logfile="$logdir/session_$timestamp.log"

mkdir -p "$logdir"

echo "# ── BrewExec Log Start: $timestamp ──" > "$logfile"

while true; do
echo -n "🧠 Prompt > "
read -r prompt

if [["$prompt" == "exit"]]; then
break
fi

echo -e "\n# Prompt: $prompt" >> "$logfile"
python3 -m brewassist_core.agents.codegen_runner "$prompt" | tee -a "$logfile"
echo
done

Now every tool (Gemini CLI, Co-P, BrewRouter, BrewAssist) has the same canonical entrypoint.

---

6. Sanity Commands for Gemini / Co-P

Once the folder rename + **init**.py are in place, these should all pass:

cd /home/brewexec
source .venv/bin/activate

# 1) Is the package visible?

python -c "import brewassist_core; print('OK', brewassist_core.**file**)"

# 2) Does the fallback chain run?

python -m brewassist_core.agents.codegen_runner 'Write a Python function to add two numbers'

# 3) Does the logger + replay pattern work?

./overlays/brewlog.sh # type a couple prompts, then 'exit'
./overlays/brewreplay.sh ~/brewexec/logs/session\_\*.log

If those three work, Test 12.2 is green and the rest of the Mistral testing matrix is unblocked.

---

7. TL;DR for Gemini

If you want a short version to hand him:

> Rename brewassist-core → brewassist_core or move the code into /home/brewexec/brewassist_core/.

Ensure:

/home/brewexec/brewassist_core/**init**.py

/home/brewexec/brewassist_core/agents/**init**.py

Run from /home/brewexec with the venv active:

python -c "import brewassist_core; print(brewassist_core.**file**)"

python -m brewassist_core.agents.codegen_runner "test"

Update all shell scripts to call python3 -m brewassist_core.agents.codegen_runner "$prompt" instead of pointing at a raw file.

That’s the combined Co-P + ChatG fix, tuned for your BrewExec layout.
