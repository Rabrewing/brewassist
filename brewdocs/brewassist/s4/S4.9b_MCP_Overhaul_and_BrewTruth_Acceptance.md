## S4.9b.x Acceptance Checklist

Before considering S4.9b.x (MCP Tools Overhaul) fully complete, the following must be true:

### 1. UI Scaffolding & Integration
- [x] New wizard components (`McpWizardModal`, `FileWizard`, `GitWizard`, `DatabaseWizard`, `ResearchWizard`) are created as stubs.
- [x] The `ActionMenu` component includes new buttons for each wizard.
- [x] Clicking a wizard button in the `ActionMenu` correctly opens its corresponding wizard modal.
- [x] `WorkspaceSidebarLeft.tsx` correctly imports and renders the wizard components.

### 2. BrewTruth API Wiring & Functionality
- [x] `pages/api/brewtruth.ts` and `pages/api/brewassist.ts` are correctly set up to interact with `lib/brewtruth.ts`.
- [x] `lib/brewtruth.ts`'s `runBrewTruth` function returns a stubbed `BrewTruthReport` with `version: "2"`.
- [x] The `BREWTRUTH_ENABLED` environment variable is correctly respected by the Next.js application (when set via `package.json` script).

### 3. Verification (Curl Tests)
- [x] **TEST 1 (Basic Truth Grading - `/api/brewtruth`):** Passes, returning `truth.version = "2"`.
- [x] **TEST 2 (BrewAssist Full Response With Truth - `/api/brewassist`):** Passes, returning a non-null `truth` object with `truth.version = "2"`.
- [x] **TEST 3 (Truth Disabled Falls Back Safely - `/api/brewassist` with `BREWTRUTH_ENABLED=false`):** Passes, returning `truth: null`.

### 4. Environment Variable Hardening
- [x] The temporary `NODE_ENV === "development"` hack has been removed from `lib/brewassist-engine.ts`.
- [x] `BREWTRUTH_ENABLED` is reliably set for the Next.js development server by explicitly including it in the `dev` script of `package.json`.
- [x] A Jest test suite (`__tests__/api/brewtruth.test.ts`) exists and passes, confirming the conditional logic for `BREWTRUTH_ENABLED` in `lib/brewassist-engine.ts`.

### 5. Future Cleanup (S4.9b.env)
- [ ] A follow-up task (S4.9b.env) is documented to address the underlying Next.js environment variable loading issue more robustly, if desired, and to remove the `package.json` script modification.

---
**Current Status: All checked items are complete.**
